<%= render :partial => 'clean_navbar' %>
<%= render :partial => 'usernavbar' %>

<h1>Donation History</h1>
<% @donation_history.each do |donation| %>
<%= "$#{donation.amount}0 to #{Charity.find(donation.charity_id).name}" %>
<br />
<% end %>